# Semantic Ownership

<!-- modality:semantic_ownership -->

Semantic ownership declares concept-level responsibility across repository path
patterns. The MVP supports manual declarations, owner/conflict queries, an
optional static seed catalog, and deterministic ingest from
`.metagit/graph/ingest-hints.json`.
