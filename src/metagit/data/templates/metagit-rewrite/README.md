# metagit-rewrite template

Bundled init template for a reference-implementation rewrite coordinator
(template id: `metagit-rewrite`).

The umbrella manifest example lives at `examples/metagit-rewrite/.metagit.yml` in the
metagit-cli repository. See `docs/metagit-rewrite-workspace.md` for the full guide.
