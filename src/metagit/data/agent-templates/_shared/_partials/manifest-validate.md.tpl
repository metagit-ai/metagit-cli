## Manifest validation

- Run `metagit config validate -c {{ manifest_path }}` after catalog or graph edits.
- Never claim work is complete without a passing validation result.
- Preview risky edits with `metagit config preview` before apply.
